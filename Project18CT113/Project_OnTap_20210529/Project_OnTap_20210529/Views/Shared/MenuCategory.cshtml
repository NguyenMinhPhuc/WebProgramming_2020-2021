@model IEnumerable<Models.EF.tblCat>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th> Danh muc</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.ActionLink(item.CatName, "Index", new { CategoryID = item.CatID })</td>
               
            </tr>
        }
    </tbody>
</table>
